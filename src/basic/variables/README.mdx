## 变量命名

JavaScript 的变量命名有两个限制：

1. 变量名称必须仅包含字母、数字、`$` 和 `_`。
2. 首字符必须非数字。

有效的命名，例如：

```js
let userName;
let test123;
```

如果命名包括多个单词，通常采用驼峰式命名法（camelCase）。也就是，单词一个接一个，除了第一个单词，其他的每个单词都以大写字母开头：myVeryLongName。

## 常量

声明一个常数（不变）变量，可以使用 `const` 而非 `let`:

